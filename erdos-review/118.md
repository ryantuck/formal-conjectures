# Erdős Problem 118 Review

## Problem Statement

Does $\alpha \to (\alpha, 3)^2$ imply $\alpha \to (\alpha, n)^2$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/118.lean`.

The theorem `erdos_118` asks if `answer(False)` corresponds to this implication. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
