# Erdős Problem 185 Review

## Problem Statement

Max size of subset of $\{0,1,2\}^n$ with no 3 points on a line.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/185.lean`.

The theorem `erdos_185` asks if `answer(True)` corresponds to $o(3^n)$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
