# Erdős Problem 129 Review

## Problem Statement

Ramsey number bounds for avoiding $K_k$ in one color.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/129.lean`.

The theorem `erdos_129` asks if `answer(False)` corresponds to the sub-exponential bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
