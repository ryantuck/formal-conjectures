# Erdős Problem 1028 Review

## Problem Statement

Estimate $H(n)=\min_f \max_{X\subseteq \{1,\ldots,n\}} \left\lvert \sum_{x
eq y\in X} f(x,y)ightvert$, where $f$ ranges over all functions $f:X^2	o \{-1,1\}$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/1028.lean`.

The function `H` is a placeholder (`sorry`). The theorem statement depends on this missing definition.

## Completeness

The formalization is incomplete.

## Confidence

0%
