# Erdős Problem 87 Review

## Problem Statement

Relationship between $R(G)$ and $R(k)$ for $\chi(G)=k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/87.lean`.

The theorem `erdos_87` asks if `answer(sorry)` corresponds to the lower bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
