# Erdős Problem 128 Review

## Problem Statement

If every large subgraph has many edges, must $G$ contain a triangle?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/128.lean`.

The theorem `erdos_128` asks if `answer(sorry)` corresponds to the implication. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
