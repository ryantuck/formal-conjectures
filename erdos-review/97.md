# Erdős Problem 97 Review

## Problem Statement

Does every convex polygon have a vertex with no other 4 vertices equidistant from it?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/97.lean`.

The theorem `erdos_97` asks if `answer(sorry)` corresponds to this property. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
