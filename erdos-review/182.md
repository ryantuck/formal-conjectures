# Erdős Problem 182 Review

## Problem Statement

Max edges in graph without $k$-regular subgraph.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/182.lean`.

The theorem `erdos_182` asks if `answer(True)` corresponds to $n^{1+o(1)}$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
