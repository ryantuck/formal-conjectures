# Erdős Problem 186 Review

## Problem Statement

Max size of non-averaging subset of $\{1, \dots, N\}$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/186.lean`.

The theorem `erdos_186` asserts the known bounds $N^{1/4 \pm \epsilon}$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
