# Erdős Problem 179 Review

## Problem Statement

Growth of $F_k(N, \ell)$, the threshold for forcing an $\ell$-term AP given density of $k$-term APs.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/179.lean`.

The theorems `erdos_179_1` and `_2` assert the asymptotic behavior. Matches.

## Completeness

The formalization uses `sorry` for the proofs, but the statements are complete.

## Confidence

100%
