# Erdős Problem 986 Review

## Problem Statement

For any fixed $k\geq 3$, $R(k,n) \gg \frac{n^{k-1}}{(\log n)^c}$ for some constant $c=c(k)>0$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/986.lean`.

The function `R` is a placeholder (`sorry`).
The theorem depends on this missing definition.

## Completeness

The formalization is incomplete.

## Confidence

0%
