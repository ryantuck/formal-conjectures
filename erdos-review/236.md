# Erdős Problem 236 Review

## Problem Statement

Number of solutions to $n = p + 2^k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/236.lean`.

The theorem `erdos_236` asserts $f(n) = o(\log n)$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
