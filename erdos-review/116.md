# Erdős Problem 116 Review

## Problem Statement

Measure of level sets of polynomials with roots on unit disk.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/116.lean`.

The theorem `erdos_116` asserts a polynomial lower bound (solved, though strong version exists). Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
