# Erdős Problem 135 Review

## Problem Statement

Number of distances if every 4 points determine 5 distances.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/135.lean`.

The theorem `erdos_135` asks if `answer(False)` corresponds to the $O(n^2)$ bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
