# Erdős Problem 273 Review

## Problem Statement

Covering system with moduli $p-1$ for $p \ge 5$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/273.lean`.

The theorem `erdos_273` asks if `answer(sorry)` corresponds to existence. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
