# Erdős Problem 21 Review

## Problem Statement

Is it true that $f(n) \ll n$, where $f(n)$ is the minimal size of a valid family?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/21.lean`.

The theorem `erdos_21` formalizes the linear bound on $f(n)$.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
