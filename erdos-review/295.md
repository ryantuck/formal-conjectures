# Erdős Problem 295 Review

## Problem Statement

Smallest $k$ for unit fraction sum 1 with denominators $\ge N$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/295.lean`.

The theorem `erdos_295` asks if $k(N) - (e-1)N \to \infty$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
