# Erdős Problem 67 Review

## Problem Statement

The Erdős discrepancy problem: Is the discrepancy of any $\pm 1$ sequence unbounded?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/67.lean`.

The theorem `erdos_67` asserts the discrepancy is unbounded (Tao's theorem). Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
