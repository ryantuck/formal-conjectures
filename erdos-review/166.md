# Erdős Problem 166 Review

## Problem Statement

Lower bound for $R(4,k)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/166.lean`.

The theorem `erdos_166` asserts the bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
