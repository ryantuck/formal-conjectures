# Erdős Problem 140 Review

## Problem Statement

Bound on $r_3(N) \ll N/(\log N)^C$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/140.lean`.

The theorem `erdos_140` asserts this bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
