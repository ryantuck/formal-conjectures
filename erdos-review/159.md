# Erdős Problem 159 Review

## Problem Statement

Ramsey number $R(C_4, K_n)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/159.lean`.

The theorem `erdos_159` asks if `answer(sorry)` corresponds to $O(n^{2-c})$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
