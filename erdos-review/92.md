# Erdős Problem 92 Review

## Problem Statement

Let $f(n)$ be the max number of equidistant points from each point in a set of size $n$. Is $f(n) \le n^{o(1)}$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/92.lean`.

The theorems `erdos_92.variants.weak` and `strong` formalize these upper bounds. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
