# Erdős Problem 90 Review

## Problem Statement

Unit distance problem: Max number of unit distances is $n^{1+o(1)}$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/90.lean`.

The theorem `erdos_90` asks if `answer(sorry)` corresponds to this upper bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
