# Erdős Problem 341 Review

## Problem Statement

Periodicity of differences in sequence avoiding $a_i+a_j$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/341.lean`.

The theorem `erdos_341` asks if `answer(sorry)` corresponds to eventual periodicity. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
