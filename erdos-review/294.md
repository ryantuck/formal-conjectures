# Erdős Problem 294 Review

## Problem Statement

Smallest $t$ such that no unit fraction sum 1 uses denominators in $[t, N]$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/294.lean`.

The theorem `erdos_294` asserts the bounds. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
