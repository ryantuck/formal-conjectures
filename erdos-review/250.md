# Erdős Problem 250 Review

## Problem Statement

Irrationality of $\sum \sigma(n)/2^n$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/250.lean`.

The theorem `erdos_250` asks if `answer(True)` corresponds to irrationality. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
