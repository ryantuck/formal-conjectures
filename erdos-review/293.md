# Erdős Problem 293 Review

## Problem Statement

Growth of $v(k)$, minimal integer not in a unit fraction sum of length $k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/293.lean`.

The theorem `erdos_293` states the existence of a function $f$ (asymptotics). Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
