# Erdős Problem 256 Review

## Problem Statement

Growth of max modulus of product of $(1-z^{a_i})$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/256.lean`.

The theorems `erdos_256` and `.belov_konyagin` formalize the negative answer and upper bound. Matches.

## Completeness

The formalization uses `sorry` for the proofs, but the statements are complete.

## Confidence

100%
