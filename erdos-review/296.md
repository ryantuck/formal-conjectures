# Erdős Problem 296 Review

## Problem Statement

Max number of disjoint subsets of $\{1, \dots, N\}$ summing to 1.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/296.lean`.

The theorem `erdos_296` asserts the asymptotic. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
