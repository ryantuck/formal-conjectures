# Erdős Problem 1035 Review

## Problem Statement

Is there a constant $c>0$ such that every graph on $2^n$ vertices with minimum degree $>(1-c)2^n$ contains the $n$-dimensional hypercube $Q_n$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/1035.lean`.

The formalization is a placeholder (`sorry`) inside the existential quantifier for the embedding `f`.

## Completeness

The formalization is incomplete.

## Confidence

0%
