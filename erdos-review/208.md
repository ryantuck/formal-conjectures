# Erdős Problem 208 Review

## Problem Statement

Upper bound on gaps between squarefree numbers.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/208.lean`.

The theorems `erdos_208.i` and `.ii` ask if `answer(sorry)` corresponds to the bounds. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
