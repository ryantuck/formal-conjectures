# Erdős Problem 94 Review

## Problem Statement

For convex polygons, is $\sum f(u_i)^2 \ll n^3$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/94.lean`.

The theorem `erdos_94` asks if `answer(sorry)` corresponds to this upper bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
