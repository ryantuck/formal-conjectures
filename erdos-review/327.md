# Erdős Problem 327 Review

## Problem Statement

Size of set where $a+b \nmid ab$ for distinct $a,b$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/327.lean`.

The theorems `erdos_327_van_doorn`, `_question1`, `_question2` formalize the results and open problems. Matches.

## Completeness

The formalization uses `sorry` for the proofs, but the statements are complete.

## Confidence

100%
