# Erdős Problem 283 Review

## Problem Statement

Representing $1 = \sum 1/n_i$ and $m = \sum p(n_i)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/283.lean`.

The theorem `erdos_283` asks if `answer(sorry)` corresponds to this property for all polynomials. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
