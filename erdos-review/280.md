# Erdős Problem 280 Review

## Problem Statement

Number of uncovered integers up to $n_k$ for fast growing $n_k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/280.lean`.

The theorem `erdos_280` asserts the negation of the conjecture. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
