# Erdős Problem 285 Review

## Problem Statement

Minimal value of $n_k$ in unit fraction sum of length $k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/285.lean`.

The theorem `erdos_285` asks if `answer(True)` corresponds to the asymptotic. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
