# Erdős Problem 106 Review

## Problem Statement

Max sum of side lengths of $n$ disjoint squares in unit square. Is $f(k^2+1)=k$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/106.lean`.

The theorem `erdos_106` asks if `answer(sorry)` corresponds to this equality. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
