# Erdős Problem 121 Review

## Problem Statement

Size of subset with no $k$ elements producting a square.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/121.lean`.

The theorem `erdos_121` asks if `answer(False)` corresponds to the density being 1. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
