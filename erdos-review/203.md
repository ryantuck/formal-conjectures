# Erdős Problem 203 Review

## Problem Statement

Is there an $m$ coprime to 6 such that $2^k 3^\ell m + 1$ is never prime?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/203.lean`.

The theorem `erdos_203` asks if `answer(sorry)` corresponds to existence. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
