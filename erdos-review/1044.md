# Erdős Problem 1044 Review

## Problem Statement

If $\Lambda(f)$ is the maximum of the lengths of the boundaries of the connected components of $\{ z: \lvert f(z)vert < 1\}$, determine the infimum of $\Lambda(f)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/1044.lean`.

The function `Λ` is a placeholder (`sorry`). The theorem statement depends on this missing definition.

## Completeness

The formalization is incomplete.

## Confidence

0%
