# Erdős Problem 325 Review

## Problem Statement

Number of integers $\le x$ expressible as sum of three $k$-th powers.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/325.lean`.

The theorem `erdos_325` asks if `answer(sorry)` corresponds to the bound $x^{3/k}$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
