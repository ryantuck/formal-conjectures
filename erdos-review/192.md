# Erdős Problem 192 Review

## Problem Statement

Three-term APs in sequences with unit step differences.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/192.lean`.

The theorem `erdos_problem_192` asserts the dimension bound $d \le 3$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
