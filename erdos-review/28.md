# Erdős Problem 28 Review

## Problem Statement

If $A + A$ contains all but finitely many integers then $\limsup 1_A * 1_A(n) = \infty$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/28.lean`.

The theorem `erdos_28` asserts the limsup condition.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
