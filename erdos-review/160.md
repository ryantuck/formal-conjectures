# Erdős Problem 160 Review

## Problem Statement

Coloring number to avoid monochromatic 4-term APs.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/160.lean`.

The theorems `erdos_160.better_upper` and `_lower` use `answer(sorry)` to ask for bounds. Matches.

## Completeness

The formalization uses `sorry` for the answer/proof, but the statement is complete.

## Confidence

100%
