# Erdős Problem 316 Review

## Problem Statement

Partitioning set with harmonic sum < 2 into two sets with sum < 1.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/316.lean`.

The theorem `erdos_316` asks if `answer(False)` corresponds to existence of partition (disproved). Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
