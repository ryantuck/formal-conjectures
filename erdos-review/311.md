# Erdős Problem 311 Review

## Problem Statement

Smallest non-zero distance from 1 for unit fraction sums.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/311.lean`.

The theorems `erdos_311_lower_bound`, `_tang_upper` assert the bounds. `erdos_311_conjecture` states the conjecture. Matches.

## Completeness

The formalization uses `sorry` for the proofs, but the statements are complete.

## Confidence

100%
