# Erdős Problem 200 Review

## Problem Statement

Length of longest AP of primes in $\{1, \dots, N\}$ is $o(\log N)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/200.lean`.

The theorem `erdos_200` asks if `answer(sorry)` corresponds to this asymptotic. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
