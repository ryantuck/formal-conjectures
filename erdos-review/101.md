# Erdős Problem 101 Review

## Problem Statement

Number of lines containing four points in a general position set is $o(n^2)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/101.lean`.

The theorem `erdos_101` asks if `answer(sorry)` corresponds to this asymptotic bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
