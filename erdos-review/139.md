# Erdős Problem 139 Review

## Problem Statement

Roth's/Szemeredi's theorem: $r_k(N) = o(N)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/139.lean`.

The theorem `erdos_139` asserts the limit is 0. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
