# Erdős Problem 304 Review

## Problem Statement

Minimal $k$ for $a/b = \sum_{i=1}^k 1/n_i$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/304.lean`.

The theorem `upper_bound` asks if $N(b) \ll \log \log b$. Matches.

## Completeness

The formalization uses `sorry` for the answer, but the statement is complete.

## Confidence

100%
