# Erdős Problem 955 Review

## Problem Statement

If $A\subset \mathbb{N}$ has density $0$ then $s^{-1}(A)$ must also have density $0$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/955.lean`.

The function `density` is a placeholder (`sorry`).
The theorem depends on this undefined function.

## Completeness

The formalization is incomplete.

## Confidence

0%
