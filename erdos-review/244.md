# Erdős Problem 244 Review

## Problem Statement

Density of $p + \lfloor C^k \rfloor$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/244.lean`.

The theorem `erdos_244` asks if `answer(sorry)` corresponds to positive density. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
