# Erdős Problem 1036 Review

## Problem Statement

Let $G$ be a graph on $n$ vertices which does not contain a trivial (empty or complete) graph on more than $c\log n$ vertices. Must $G$ contain at least $2^{\Omega_c(n)}$ many induced subgraphs which are not pairwise isomorphic?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/1036.lean`.

The formalization is a placeholder (`sorry`) in the conclusion.

## Completeness

The formalization is incomplete.

## Confidence

0%
