# Erdős Problem 1014 Review

## Problem Statement

Let $R(k,l)$ be the Ramsey number. Prove, for fixed $k\geq 3$, that $\lim_{l	o \infty}\frac{R(k,l+1)}{R(k,l)}=1$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/1014.lean`.

The function `R` is a placeholder (`sorry`). The theorem statement depends on this missing definition.

## Completeness

The formalization is incomplete.

## Confidence

0%
