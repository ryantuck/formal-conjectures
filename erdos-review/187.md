# Erdős Problem 187 Review

## Problem Statement

Function $f(d)$ for monochromatic APs with difference $d$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/187.lean`.

The theorem `erdos_187` asserts existence of best function and non-existence of $\log d$ bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
