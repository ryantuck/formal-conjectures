# Erdős Problem 181 Review

## Problem Statement

Ramsey number of hypercube $Q_n$ is $O(2^n)$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/181.lean`.

The theorem `erdos_181` asserts the bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
