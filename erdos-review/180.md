# Erdős Problem 180 Review

## Problem Statement

Relation between $\mathrm{ex}(n; G)$ and $\mathrm{ex}(n; \mathcal{F})$ for $G \in \mathcal{F}$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/180.lean`.

The theorem `erdos_180` asks if `answer(False)` corresponds to the relation holding universally. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
