# Erdős Problem 912 Review

## Problem Statement

If $n! = \prod_i p_i^{k_i}$, let $h(n)$ count the number of distinct exponents $k_i$. Prove that there exists some $c>0$ such that $h(n) \sim c \left(\frac{n}{\log n}ight)^{1/2}$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/912.lean`.

The function `h(n)` is defined as `(n!).factorization.frange.card`. Assuming standard Mathlib semantics where `factorization` returns a `Finsupp` and `frange` returns the range of values (exponents), this is correct.
The theorems correctly state the asymptotic equivalence.

## Completeness

The formalization is complete and accurate.

## Confidence

100%
