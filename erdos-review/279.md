# Erdős Problem 279 Review

## Problem Statement

Covering large integers with $a_p + tp$ for $t \ge k$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/279.lean`.

The theorem `erdos_279` asks if such a system exists covering all large $n$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
