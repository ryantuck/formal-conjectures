# Erdős Problem 278 Review

## Problem Statement

Minimum density of union of arithmetic progressions with moduli $n_i$.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/278.lean`.

The theorem `erdos_278.simpson` asserts the bound using the formula. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
