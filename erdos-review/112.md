# Erdős Problem 112 Review

## Problem Statement

Value of $k(n,m)$ for directed graphs (independent set or transitive tournament).

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/112.lean`.

The theorem `erdos_112` asks for the value of $k(5,5)$. Matches.

## Completeness

The formalization uses `sorry` for the proof/value, but the statement is complete.

## Confidence

100%
