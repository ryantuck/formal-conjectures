# Erdős Problem 111 Review

## Problem Statement

Behavior of edge deletions to make subgraphs bipartite for graphs with infinite chromatic number.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/111.lean`.

The theorem `erdos_111` asks if `answer(sorry)` corresponds to $h_G(n)/n \to \infty$. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
