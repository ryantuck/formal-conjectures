# Erdős Problem 300 Review

## Problem Statement

Max size of subset of $\{1, \dots, N\}$ with no subset summing to 1.

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/300.lean`.

The theorem `erdos_300` asserts the linear bound. Matches.

## Completeness

The formalization uses `sorry` for the proof, but the statement is complete.

## Confidence

100%
