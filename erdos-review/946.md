# Erdős Problem 946 Review

## Problem Statement

Are there infinitely many $n$ such that $	au(n)=	au(n+1)$?

## Formalization

The problem is formalized in `FormalConjectures/ErdosProblems/946.lean`.

The theorem `erdos_946` correctly asserts the infinitude of such $n$, using `σ 0` for divisor count.

## Completeness

The formalization is complete and accurate.

## Confidence

100%
