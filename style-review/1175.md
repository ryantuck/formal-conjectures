# Style Review: Erdős Problem 1175

**File:** `FormalConjectures/ErdosProblems/1175.lean`
**Review Date:** 2026-02-11
**Status:** ⚠️ **NEEDS FIX** - Question format and vacuous `True`

## Summary
Problem asks "Must there exist...?" but uses statement format. Statement contains multiple vacuous `True` conditions.

**Score:** 15/21 passing

## Critical Issues

1. **Question format** - ❌ Docstring: "Must there exist..." → should use `answer(sorry) ↔`

2. **Vacuous conditions** - ❌ Lines 47, 49, 52:
   ```lean
   ∀ (V : Type*) (G : SimpleGraph V),
     True →  -- Vacuous!
     ∃ (H : SimpleGraph V), H ≤ G ∧
       (∀ (tri : Finset V), tri.card = 3 → ¬H.IsClique (tri : Set V)) ∧
       True  -- Vacuous!
   ```

   These `True` placeholders should encode chromatic number conditions.

3. **Incomplete disjunction** - Second branch also has vacuous `True`

## Current vs. Correct

**Current:**
```lean
theorem triangle_free_subgraph_chromatic :
    ∀ (kappa_type : Type*) [Infinite kappa_type],
      (∃ (lambda_type : Type*), ...) ∨
      (∀ (lambda_type : Type*), ...) := by sorry
```

**Should be:**
```lean
theorem triangle_free_subgraph_chromatic :
    answer(sorry) ↔
      (∀ (kappa_type : Type*) [Infinite kappa_type],
        ∃ (lambda_type : Type*), ...[proper conditions]...) := by sorry
```

## Good Aspects
- ✅ Triangle-free condition properly stated
- ✅ Uses H ≤ G for subgraph
- ✅ Mentions Shelah's consistency result

## Recommendation
**Priority 1:** Replace all vacuous `True` with proper chromatic number conditions
**Priority 2:** Fix question format
